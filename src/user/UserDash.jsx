import React from "react";
import Layout from "../core/Layout";
import { isAuthenticated } from "../auth";

const Dash = () => {
    return (
        <Layout title="Dash" description="User Dash">
            ...
        </Layout>
    );
};

export default Dash;
